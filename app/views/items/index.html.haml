#index_items
  %div.center
    - if current_user.admin
      %h3= link_to "Add item", new_item_path
    %h2
      - @items.each do |item|
        %h3{class: "col-xs-4"} #{link_to item.name , item}: #{number_to_currency item.price}